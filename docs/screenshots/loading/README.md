# Loading states

Unretouched captures of the real console with synthetic, local-only data and
three seconds of injected API latency, September 22, 2026. No production data
or credentials are present. The same accessible orbit component is used during
initial startup, page loads, refreshes, machine details and remote startup.
Animation respects the operating system's reduced-motion preference.

| Desktop | Mobile | Machine details |
| --- | --- | --- |
| ![Fleet loading](fleet-loading.png) | ![Mobile fleet loading](mobile-loading.png) | ![Machine update loading](drawer-loading.png) |
